Run `npx prisma generate` after `npm install` (needs internet access to
binaries.prisma.sh — see root README for why that's a no-op in this
sandbox but fine on a real machine), then `npx prisma migrate dev` to
create the schema.
